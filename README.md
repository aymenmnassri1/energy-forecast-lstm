# energy-forecast-lstm
hort-term photovoltaic (PV) power forecasting using machine learning and deep learning models (LSTM, GRU, TCN, XGBoost). The project uses real 5-minute PV data (3-month subset) and integrates predictions into an embedded Energy Management System (EMS) under export constraints.
